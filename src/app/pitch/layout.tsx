export const metadata = {
  title: "GKS-for-U | Pitch Deck",
  description: "Interactive pitch deck for GKS scholarship application helper",
};

export default function PitchLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
